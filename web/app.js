const app = document.getElementById('app');
const state = { me:null, plans:null, dash:null, current:null };
const fmt = n => new Intl.NumberFormat('en-IN').format(Number(n||0));
const money = n => `₹${fmt(n)}`;
const esc = v => String(v ?? '').replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const qs = (s,r=document)=>r.querySelector(s);
const qsa = (s,r=document)=>[...r.querySelectorAll(s)];
function toast(msg){const el=document.createElement('div');el.className='toast';el.textContent=msg;document.body.appendChild(el);setTimeout(()=>el.remove(),3200)}
async function copyText(text){try{await navigator.clipboard.writeText(text);toast('Copied')}catch{toast('Copy failed')}}
async function api(path, options={}){const init={credentials:'include',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}};if(init.body&&typeof init.body!=='string')init.body=JSON.stringify(init.body);const res=await fetch(path,init);let data=null;try{data=await res.json()}catch{data={}}if(!res.ok){const d=data?.detail;const msg=typeof d==='string'?d:(d?.message||data?.error||`Request failed (${res.status})`);const e=new Error(msg);e.status=res.status;e.data=data;throw e}return data}
function go(path){history.pushState({},'',path);renderRoute()}
function brand(){return `<a class="brand" data-link href="/"><img src="/assets/mark.svg" width="38" height="38" alt=""><div class="brand-name">INTERNET <span>HANDS</span></div></a>`}
function nav(){return `<nav class="site-nav"><div class="nav-inner">${brand()}<div class="nav-links"><a data-link href="/docs">Docs</a><a data-link href="/pricing">Pricing</a><a data-link href="/status">Status</a><a href="https://github.com/sphangcho203-afk/Web-Scrapping-CLI" target="_blank" rel="noreferrer">GitHub</a></div><div class="nav-actions"><a data-link class="btn btn-ghost" href="/login">Sign in</a><a data-link class="btn btn-primary" href="/signup">Get started</a></div></div></nav>`}
function footer(){return `<footer class="footer"><div class="container footer-grid"><div>${brand()}</div><div>One MCP. An entire capability fabric.</div><div>© ${new Date().getFullYear()} Internet Hands</div></div></footer>`}
function publicShell(body){app.innerHTML=`${nav()}${body}${footer()}`;wireLinks()}
function wireLinks(){qsa('[data-link]').forEach(a=>a.addEventListener('click',e=>{if(e.metaKey||e.ctrlKey||e.shiftKey)return;e.preventDefault();go(a.getAttribute('href'))}))}
function feature(k,t,d){return `<article class="feature"><div class="feature-kicker">${k}</div><h3>${t}</h3><p>${d}</p></article>`}
async function getPlans(){if(state.plans)return state.plans;try{state.plans=await api('/api/public/plans')}catch{state.plans={plans:[{slug:'free',name:'Free',monthly_price_inr:0,included_credits:2500,rpm_limit:10,api_key_limit:1,monitor_limit:1},{slug:'builder',name:'Builder',monthly_price_inr:499,included_credits:25000,rpm_limit:60,api_key_limit:5,monitor_limit:10},{slug:'pro',name:'Pro',monthly_price_inr:1499,included_credits:150000,rpm_limit:240,api_key_limit:20,monitor_limit:50},{slug:'scale',name:'Scale',monthly_price_inr:4999,included_credits:750000,rpm_limit:600,api_key_limit:100,monitor_limit:250}],credit_packs:[]}}return state.plans}
function architecture(){return `<div class="fabric"><div class="fabric-core">IH / MCP</div><div class="node n1"><strong>Firecrawl</strong>search · scrape · crawl</div><div class="node n2"><strong>Tool Mesh</strong>OpenAPI · MCP · APIs</div><div class="node n3"><strong>Gaming Intel</strong>profile · rank · mains</div><div class="node n4"><strong>Cloud Computer</strong>browser · shell · files</div><div class="node n5"><strong>Apify</strong>actors · datasets</div><div class="node n6"><strong>Monitors</strong>web · API · gaming</div></div>`}
async function renderHome(){const pdata=await getPlans();publicShell(`<main><section class="hero"><div class="container hero-grid"><div><span class="eyebrow"><i class="pulse"></i> MCP + API capability fabric</span><h1>Give your agent <em>hands</em> on the internet.</h1><p class="hero-copy">Internet Hands unifies live web intelligence, browser computers, gaming intelligence, APIs, scrapers and remote MCPs behind one authenticated endpoint with credits, monitoring and routing.</p><div class="hero-actions"><a data-link href="/signup" class="btn btn-primary">Start free</a><a data-link href="/docs" class="btn">Read the docs</a></div><div class="hero-meta"><span><strong>1 endpoint</strong> for the agent</span><span><strong>runtime discovery</strong> instead of giant prompts</span><span><strong>metered</strong> by tool cost</span></div></div>${architecture()}</div></section><section class="section"><div class="container"><div class="section-head"><div><span class="eyebrow">Capability planes</span><h2>Small context. Huge reach.</h2></div><p>The model discovers what it needs, pulls only the relevant schema, and routes the call through the best configured provider.</p></div><div class="grid3">${feature('WEB','Live web intelligence','Search, fetch, map, crawl, structured extraction and research through Firecrawl, Apify and native Internet Hands tooling.')}${feature('COMPUTER','Disposable cloud computers','Persistent Chromium sessions, terminal commands, packages, repositories, files, services, snapshots and artifacts.')}${feature('GAMING','Gaming intelligence','Resolve player identities and enrich public profiles with rank, MMR, most-used heroes/champions, matches and progression.')}${feature('MESH','API federation','RapidAPI manifests, OpenAPI catalogs, public APIs, connected apps and remote MCP servers normalize into one tool reference format.')}${feature('OBSERVE','Monitors','Track public web pages, APIs, MCP endpoints and game identities with quota-aware monitoring and historical runs.')}${feature('CONTROL','Usage control','Scoped API keys, short-lived OAuth access tokens, per-plan RPM, wallet credits, request IDs and complete usage ledgers.')}</div></div></section><section class="section"><div class="container"><div class="section-head"><div><span class="eyebrow">Developer first</span><h2>Connect once.</h2></div><p>The canonical MCP URL stays stable while the capability surface evolves behind it.</p></div><div class="code-panel"><div class="code-tabs"><button class="code-tab active">ChatGPT / MCP</button><button class="code-tab">HTTP</button><button class="code-tab">Python</button></div><pre><span class="syntax-accent">MCP URL</span>\n${location.origin}/mcp\n\nAuthentication\nOAuth 2.1 + PKCE  →  Internet Hands authorization\nDirect API key    →  Authorization: Bearer ih_live_…\n\nDiscovery flow\nmesh_route → mesh_describe → mesh_execute</pre></div></div></section><section class="section"><div class="container"><div class="section-head"><div><span class="eyebrow">Pricing</span><h2>Credits follow the work.</h2></div><p>Light metadata calls stay cheap. Browser, crawl and agent jobs spend more credits based on actual work.</p></div>${pricingCards(pdata.plans)}</div></section></main>`);wireLinks()}
function pricingCards(plans){return `<div class="pricing-grid">${plans.map(p=>`<article class="price-card ${p.slug==='pro'?'featured':''}"><div class="plan-name">${esc(p.name)}</div><div class="price">${money(p.monthly_price_inr)} <small>/ month</small></div><div class="plan-desc">${fmt(p.included_credits)} included credits each billing period.</div><ul class="checklist"><li>${fmt(p.rpm_limit)} requests / min</li><li>${fmt(p.api_key_limit)} API key${p.api_key_limit===1?'':'s'}</li><li>${fmt(p.monitor_limit)} monitor${p.monitor_limit===1?'':'s'}</li><li>${p.browser_enabled?'Browser access':'Public/read-only tools'}</li><li>${p.sandbox_enabled?'Sandbox access':'Sandbox not included'}</li></ul><a data-link href="${p.slug==='free'?'/signup':'/signup?plan='+p.slug}" class="btn ${p.slug==='pro'?'btn-primary':''}">${p.slug==='free'?'Start free':'Choose '+esc(p.name)}</a></article>`).join('')}</div>`}
async function renderPricing(){const p=await getPlans();publicShell(`<main class="section"><div class="container"><div class="section-head"><div><span class="eyebrow">Plans</span><h2>Infrastructure pricing without mystery.</h2></div><p>Purchased credits roll over. Monthly plan credits refresh each billing period. Provider-heavy jobs can add metered usage.</p></div>${pricingCards(p.plans)}<div style="height:38px"></div><div class="card"><div class="card-title"><h3>Default credit costs</h3></div><div class="table-wrap"><table class="table"><thead><tr><th>Work</th><th>Base cost</th></tr></thead><tbody><tr><td>Discovery / schema / health</td><td>1 credit</td></tr><tr><td>Simple gaming or public API lookup</td><td>2 credits</td></tr><tr><td>Normalized gaming profile</td><td>5 credits + provider usage</td></tr><tr><td>Live web search</td><td>3 credits</td></tr><tr><td>Single page fetch / scrape</td><td>5 credits</td></tr><tr><td>Site map</td><td>6 credits</td></tr><tr><td>Crawl</td><td>10 + 1/page</td></tr><tr><td>Async provider agent</td><td>20+ credits</td></tr><tr><td>Browser action</td><td>2 credits</td></tr><tr><td>Sandbox command</td><td>2 credits</td></tr></tbody></table></div></div></div></main>`);wireLinks()}
async function renderStatus(){let s={};try{s=await api('/api/status')}catch{}publicShell(`<main class="section"><div class="container"><div class="section-head"><div><span class="eyebrow">System status</span><h2>Internet Hands control plane</h2></div><p>Public configuration state only. Provider credentials and customer secrets are never returned.</p></div><div class="grid3">${statusCard('MCP Gateway',true,'/mcp')}${statusCard('Control database',!!s.control_database,'Accounts · credits · usage')}${statusCard('OAuth authorization',!!s.oauth,'PKCE authorization code')}${statusCard('Razorpay billing',!!s.billing,s.billing?'Configured':'Configuration pending')}${statusCard('GitHub sign-in',!!s.github_oauth,s.github_oauth?'Configured':'Configuration pending')}${statusCard('Monitoring',true,'CRUD + quota model')}</div></div></main>`);wireLinks()}
function statusCard(name,ok,desc){return `<div class="feature"><span class="pill ${ok?'good':'warn'}">${ok?'Operational':'Pending config'}</span><h3>${name}</h3><p>${desc}</p></div>`}
const docSections={
'getting-started':{title:'Getting started',body:`<p>Internet Hands gives an agent one MCP endpoint and a capability router behind it.</p><div class="endpoint"><code>${location.origin}/mcp</code><button class="btn btn-sm" data-copy="${location.origin}/mcp">Copy</button></div><h2>1. Create an account</h2><p>Start on the Free plan. A monthly wallet is created automatically.</p><h2>2. Create an API key</h2><p>Dashboard → API Keys → New key. The full secret is displayed once and only its hash is stored.</p><h2>3. Connect your MCP client</h2><p>Use the endpoint above. OAuth-capable clients can follow the server metadata and open the Internet Hands authorization screen. Direct integrations can send the API key as a Bearer token.</p><div class="callout">Recommended agent pattern: <span class="mono">mesh_route → mesh_describe → mesh_execute</span>. Discover on demand instead of loading thousands of schemas into prompt context.</div>`},
'authentication':{title:'Authentication',body:`<p>Internet Hands supports direct API keys and OAuth 2.1-style authorization code flow with PKCE.</p><h2>Direct key</h2><pre>Authorization: Bearer ih_live_…\n# or\nX-API-Key: ih_live_…</pre><h2>OAuth discovery</h2><pre>GET /.well-known/oauth-protected-resource\nGET /.well-known/oauth-authorization-server</pre><p>The authorization screen validates your API key and exchanges it for a short-lived bearer token. Raw keys are never stored in OAuth records.</p>`},
'mcp':{title:'MCP',body:`<p>The permanent Streamable HTTP endpoint is:</p><div class="endpoint"><code>${location.origin}/mcp</code><button class="btn btn-sm" data-copy="${location.origin}/mcp">Copy</button></div><h2>Tool families</h2><ul><li>Tool mesh discovery and execution</li><li>Web intelligence and provider jobs</li><li>Gaming intelligence</li><li>Cloud computer and persistent browser</li><li>Account usage, wallet and monitor metadata</li></ul>`},
'credits':{title:'Credits & billing',body:`<p>Credits meter actual capability work. The gateway checks balance and plan rate limits before a metered MCP call is accepted.</p><h2>Two wallet buckets</h2><p>Monthly credits reset with the plan period. Purchased credits roll over and are consumed after monthly credits.</p><h2>Payments</h2><p>Razorpay orders are priced server-side. Credits or plans activate only after a verified checkout signature and confirmed/captured payment state.</p>`},
'gaming':{title:'Gaming intelligence',body:`<p>Public/read-only intelligence across MLBB, VALORANT, League, TFT, Steam, Dota 2, Hypixel, HoYo titles, Roblox, osu! and Supercell games.</p><h2>One-call profile</h2><pre>gaming_profile(game, identity)\n→ identity\n→ profile\n→ rank / MMR\n→ mains / most-used\n→ recent form\n→ titles / progression</pre>`},
'errors':{title:'Errors',body:`<p>Gateway errors are stable machine-readable codes.</p><ul><li><span class="mono">invalid_api_key</span></li><li><span class="mono">insufficient_credits</span></li><li><span class="mono">rate_limited</span></li><li><span class="mono">scope_denied</span></li><li><span class="mono">provider_unavailable</span></li><li><span class="mono">tool_not_found</span></li></ul><p>Every metered request receives an <span class="mono">X-Request-ID</span>.</p>`}
};
function renderDocs(){const slug=location.pathname.split('/')[2]||'getting-started';const d=docSections[slug]||docSections['getting-started'];publicShell(`<main class="doc-shell"><aside class="doc-nav">${Object.entries(docSections).map(([k,v])=>`<a data-link class="${k===slug?'active':''}" href="/docs/${k}">${v.title}</a>`).join('')}</aside><article class="doc"><span class="eyebrow">Documentation</span><h1>${d.title}</h1>${d.body}</article></main>`);wireLinks();qsa('[data-copy]').forEach(b=>b.onclick=()=>copyText(b.dataset.copy))}
function renderAuth(mode){const signup=mode==='signup';const params=new URLSearchParams(location.search);const gh=params.get('github');app.innerHTML=`<main class="auth-wrap"><section class="auth-art"><div>${brand()}</div><div><span class="eyebrow">Internet Hands</span><h1>One endpoint for your agent's entire capability fabric.</h1><p class="hero-copy">Accounts, API keys, credits, usage, monitoring and MCP authorization live here.</p></div><div class="muted small">Scoped credentials · metered requests · auditable usage</div></section><section class="auth-panel"><div class="auth-card"><h2>${signup?'Create account':'Welcome back'}</h2><p>${signup?'Start with 2,500 free monthly credits.':'Sign in to your developer console.'}</p>${gh?`<div class="callout">GitHub sign-in: ${gh==='config_required'?'configuration is still required on the server.':esc(gh)}</div><br>`:''}<a class="github-btn" href="/api/auth/github/start">Continue with GitHub</a><div class="divider">or use email</div><form id="auth-form" class="form-grid">${signup?`<div class="field"><label>Display name</label><input class="input" name="display_name" autocomplete="name"></div>`:''}<div class="field"><label>Email</label><input class="input" required type="email" name="email" autocomplete="email"></div><div class="field"><label>Password</label><input class="input" required minlength="8" type="password" name="password" autocomplete="${signup?'new-password':'current-password'}"></div><button class="btn btn-primary" type="submit">${signup?'Create account':'Sign in'}</button></form><p class="small muted" style="margin-top:18px">${signup?'Already have an account? <a data-link href="/login">Sign in</a>':'New here? <a data-link href="/signup">Create an account</a>'}</p></div></section></main>`;wireLinks();qs('#auth-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{await api(signup?'/api/auth/signup':'/api/auth/login',{method:'POST',body:Object.fromEntries(f.entries())});go('/dashboard')}catch(err){toast(err.message)}}}
const sideItems=[['overview','◇','Overview'],['api-keys','⌁','API Keys'],['usage','▥','Usage'],['wallet','◫','Wallet'],['billing','₹','Billing'],['monitors','◉','Monitors'],['connections','⎈','Connections'],['mcp','⌘','MCP'],['settings','⚙','Settings']];
async function ensureMe(){if(state.me)return state.me;try{state.me=await api('/api/auth/me');return state.me}catch(e){if(e.status===401){go('/login');return null}throw e}}
function dashboardShell(active,inner){const me=state.me?.user||{};app.innerHTML=`<div class="app-shell"><aside class="sidebar">${brand()}<div class="side-group"><div class="side-label">Console</div>${sideItems.map(([id,icon,label])=>`<a data-link href="/dashboard/${id==='overview'?'':id}" class="side-link ${id===active?'active':''}"><b class="side-icon">${icon}</b><span>${label}</span></a>`).join('')}</div><div class="sidebar-bottom"><div class="account-mini small muted">${esc(me.email||'')}</div><button id="logout" class="btn btn-ghost btn-sm" style="width:100%;margin-top:9px">Sign out</button></div></aside><section class="main"><header class="topbar"><div class="topbar-title">${sideItems.find(x=>x[0]===active)?.[2]||'Console'}</div><div class="avatar">${esc((me.display_name||me.email||'I').slice(0,1).toUpperCase())}</div></header><div class="content">${inner}</div></section></div>`;wireLinks();qs('#logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});state.me=null;go('/')}}
function stat(label,value,sub=''){return `<div class="stat"><div class="stat-label">${label}</div><div class="stat-value">${value}</div><div class="stat-sub">${sub}</div></div>`}
async function renderDashboard(){const me=await ensureMe();if(!me)return;const slug=location.pathname.split('/')[2]||'overview';if(slug==='overview')return dashOverview();if(slug==='api-keys')return dashKeys();if(slug==='usage')return dashUsage();if(slug==='wallet')return dashWallet();if(slug==='billing')return dashBilling();if(slug==='monitors')return dashMonitors();if(slug==='connections')return dashConnections();if(slug==='mcp')return dashMcp();return dashSettings()}
async function dashOverview(){let d;try{d=await api('/api/dashboard')}catch(e){dashboardShell('overview',`<div class="empty">${esc(e.message)}</div>`);return}state.dash=d;const a=d.account||{},u=d.usage||{},series=d.series||[];const max=Math.max(1,...series.map(x=>Number(x.calls)));dashboardShell('overview',`<div class="page-head"><div><h1>Mission control</h1><p>Live account, usage and capability health.</p></div><span class="pill good">${esc(a.plan_name||'Free')} plan</span></div><div class="stats">${stat('Credits remaining',fmt(Number(a.monthly_credits||0)+Number(a.purchased_credits||0)),`${fmt(a.monthly_credits)} monthly · ${fmt(a.purchased_credits)} purchased`)}${stat('Calls · 24h',fmt(u.calls_24h),'MCP + API')}${stat('Success · 30d',`${Number(u.success_rate||100).toFixed(1)}%`,'accepted gateway requests')}${stat('Average latency',`${fmt(u.avg_latency_ms)} ms`,'30 day average')}</div><div class="dashboard-grid"><div class="card"><div class="card-title"><h3>30-day request volume</h3><span class="small muted">${fmt(u.calls_30d)} calls</span></div><div class="usage-chart">${series.length?series.map(x=>`<div class="bar" title="${x.day}: ${x.calls}" style="height:${Math.max(2,Number(x.calls)/max*100)}%"></div>`).join(''):'<div class="empty" style="width:100%">No calls yet</div>'}</div></div><div class="card"><div class="card-title"><h3>Wallet</h3><a data-link href="/dashboard/wallet" class="small muted">View ledger →</a></div><div class="wallet-balance"><div><div class="muted small">Monthly</div><div class="stat-value">${fmt(a.monthly_credits)}</div></div><div><div class="muted small">Purchased</div><div class="stat-value">${fmt(a.purchased_credits)}</div></div></div></div></div><div style="height:16px"></div><div class="card"><div class="card-title"><h3>Most used tools</h3><a data-link href="/dashboard/usage" class="small muted">All usage →</a></div>${(d.top_tools||[]).length?`<div class="table-wrap"><table class="table"><thead><tr><th>Tool</th><th>Calls</th><th>Credits</th></tr></thead><tbody>${d.top_tools.map(x=>`<tr><td class="mono">${esc(x.tool_ref)}</td><td>${fmt(x.calls)}</td><td>${fmt(x.credits)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">Make your first MCP call and activity will appear here.</div>'}</div>`);wireLinks()}
async function dashKeys(){const data=await api('/api/api-keys');dashboardShell('api-keys',`<div class="page-head"><div><h1>API keys</h1><p>Secrets are shown once. Only hashes are stored afterward.</p></div><button id="new-key" class="btn btn-primary">New key</button></div>${data.keys.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Name</th><th>Prefix</th><th>Environment</th><th>Last used</th><th>Status</th><th></th></tr></thead><tbody>${data.keys.map(k=>`<tr><td>${esc(k.name)}</td><td class="mono">${esc(k.prefix)}…</td><td>${esc(k.environment)}</td><td>${k.last_used_at?new Date(k.last_used_at).toLocaleString():'Never'}</td><td><span class="pill ${k.revoked_at?'bad':'good'}">${k.revoked_at?'Revoked':'Active'}</span></td><td>${k.revoked_at?'':`<button class="btn btn-danger btn-sm" data-revoke="${k.id}">Revoke</button>`}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No API keys yet.</div>'}`);qs('#new-key').onclick=()=>showKeyModal();qsa('[data-revoke]').forEach(b=>b.onclick=async()=>{if(!confirm('Revoke this key? Existing clients will stop working.'))return;await api(`/api/api-keys/${b.dataset.revoke}/revoke`,{method:'POST'});dashKeys()})}
function showKeyModal(){const wrap=document.createElement('div');wrap.className='modal-backdrop';wrap.innerHTML=`<div class="modal"><div class="card-title"><h3>Create API key</h3><button class="btn btn-sm" data-close>×</button></div><form id="key-form" class="form-grid"><div class="field"><label>Name</label><input class="input" name="name" value="Agent key" required></div><div class="field"><label>Environment</label><select class="select" name="environment"><option value="live">Live</option><option value="test">Test</option></select></div><button class="btn btn-primary">Create key</button></form></div>`;document.body.appendChild(wrap);wrap.querySelector('[data-close]').onclick=()=>wrap.remove();wrap.querySelector('#key-form').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await api('/api/api-keys',{method:'POST',body:Object.fromEntries(f.entries())});wrap.innerHTML=`<div class="modal"><h3>Save this key now</h3><p class="muted">It will not be shown again.</p><div class="secret-box">${esc(r.secret)}</div><div style="display:flex;gap:10px;margin-top:16px"><button class="btn btn-primary" data-copykey>Copy secret</button><button class="btn" data-done>Done</button></div></div>`;wrap.querySelector('[data-copykey]').onclick=()=>copyText(r.secret);wrap.querySelector('[data-done]').onclick=()=>{wrap.remove();dashKeys()}}catch(err){toast(err.message)}}}
async function dashUsage(){const d=await api('/api/usage?limit=250');dashboardShell('usage',`<div class="page-head"><div><h1>Usage</h1><p>Request IDs, credits, provider routing and latency.</p></div></div>${d.events.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Time</th><th>Request</th><th>Tool</th><th>Provider</th><th>Status</th><th>Credits</th><th>Latency</th></tr></thead><tbody>${d.events.map(x=>`<tr><td>${new Date(x.created_at).toLocaleString()}</td><td class="mono">${esc(x.request_id).slice(0,18)}…</td><td class="mono">${esc(x.tool_ref||'')}</td><td>${esc(x.provider||'—')}</td><td><span class="pill ${x.status==='ok'?'good':x.status==='accepted'?'warn':'bad'}">${esc(x.status)}</span></td><td>${fmt(x.credits_charged)}</td><td>${x.latency_ms==null?'—':fmt(x.latency_ms)+' ms'}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No metered requests yet.</div>'}`)}
async function dashWallet(){const [w,p]=await Promise.all([api('/api/wallet?limit=150'),getPlans()]);const wallet=w.wallet||{};dashboardShell('wallet',`<div class="page-head"><div><h1>Wallet</h1><p>Monthly credits spend first. Purchased credits roll over.</p></div></div><div class="wallet-balance"><div class="wallet-bucket"><div class="muted small">MONTHLY CREDITS</div><div class="amount">${fmt(wallet.monthly_credits)}</div><div class="muted small">Refresh with billing period</div></div><div class="wallet-bucket"><div class="muted small">PURCHASED CREDITS</div><div class="amount">${fmt(wallet.purchased_credits)}</div><div class="muted small">Roll over until used</div></div></div><h3 style="margin:30px 0 14px">Top up</h3><div class="credit-packs">${(p.credit_packs||[]).map(x=>`<div class="credit-pack"><h3>${esc(x.name)}</h3><div class="credits">${fmt(x.credits)}</div><div class="price">${money(x.price_inr)}</div><button class="btn btn-primary btn-sm" data-buy-pack="${x.slug}">Buy credits</button></div>`).join('')||'<div class="empty">Credit packs unavailable.</div>'}</div><h3 style="margin:30px 0 14px">Ledger</h3>${w.ledger.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Time</th><th>Kind</th><th>Bucket</th><th>Amount</th><th>Source</th></tr></thead><tbody>${w.ledger.map(x=>`<tr><td>${new Date(x.created_at).toLocaleString()}</td><td>${esc(x.kind)}</td><td>${esc(x.bucket)}</td><td style="color:${Number(x.amount)>=0?'var(--good)':'var(--text)'}">${Number(x.amount)>0?'+':''}${fmt(x.amount)}</td><td>${esc(x.source)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No wallet activity yet.</div>'}`);qsa('[data-buy-pack]').forEach(b=>b.onclick=()=>startCheckout('credits',b.dataset.buyPack))}
async function dashBilling(){const [p,s,pay]=await Promise.all([getPlans(),api('/api/billing/status'),api('/api/billing/payments')]);dashboardShell('billing',`<div class="page-head"><div><h1>Billing</h1><p>Plans, top-ups and verified Razorpay payments.</p></div><span class="pill ${s.configured?'good':'warn'}">Razorpay ${s.configured?'ready':'configuration pending'}</span></div>${pricingCards(p.plans)}<h3 style="margin:32px 0 14px">Payment history</h3>${pay.payments.length?`<div class="table-wrap"><table class="table"><thead><tr><th>Created</th><th>Purpose</th><th>Amount</th><th>Status</th><th>Order</th></tr></thead><tbody>${pay.payments.map(x=>`<tr><td>${new Date(x.created_at).toLocaleString()}</td><td>${esc(x.purpose)}</td><td>${money(Number(x.amount_paise)/100)}</td><td><span class="pill ${['paid','captured'].includes(x.status)?'good':'warn'}">${esc(x.status)}</span></td><td class="mono">${esc(x.order_id||'—')}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No payments yet.</div>'}`);qsa('.price-card a').forEach(a=>{const href=a.getAttribute('href')||'';if(href.includes('plan=')){a.onclick=e=>{e.preventDefault();startCheckout('subscription',new URL(href,location.origin).searchParams.get('plan'))}}})}
async function loadRazorpay(){if(window.Razorpay)return;await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://checkout.razorpay.com/v1/checkout.js';s.onload=resolve;s.onerror=reject;document.head.appendChild(s)})}
async function startCheckout(purpose,slug){try{const r=await api('/api/billing/create-order',{method:'POST',body:{purpose,slug}});await loadRazorpay();const rz=new Razorpay({key:r.key_id,order_id:r.order.id,amount:r.order.amount,currency:'INR',name:'Internet Hands',description:purpose==='credits'?'Credit top-up':'Internet Hands plan',handler:async resp=>{try{await api('/api/billing/verify',{method:'POST',body:resp});toast('Payment verified');purpose==='credits'?dashWallet():dashBilling()}catch(e){toast(e.message)}},theme:{color:'#73e5ef'}});rz.open()}catch(e){toast(e.message)}}
async function dashMonitors(){const d=await api('/api/monitors');dashboardShell('monitors',`<div class="page-head"><div><h1>Monitors</h1><p>Track public pages, APIs, MCP endpoints and gaming identities.</p></div><button id="new-monitor" class="btn btn-primary">New monitor</button></div><div class="monitor-list">${d.monitors.length?d.monitors.map(m=>`<div class="monitor-row"><div><strong>${esc(m.name)}</strong><small>${esc(m.type)} · ${esc(m.target)}</small></div><span class="pill ${m.last_status==='ok'?'good':'warn'}">${m.last_status||'Not run'}</span><button class="btn btn-sm" data-toggle="${m.id}" data-enabled="${m.enabled}">${m.enabled?'Pause':'Enable'}</button></div>`).join(''):'<div class="empty">No monitors yet.</div>'}</div>`);qs('#new-monitor').onclick=showMonitorModal;qsa('[data-toggle]').forEach(b=>b.onclick=async()=>{await api(`/api/monitors/${b.dataset.toggle}/toggle`,{method:'POST',body:{enabled:b.dataset.enabled!=='true'}});dashMonitors()})}
function showMonitorModal(){const wrap=document.createElement('div');wrap.className='modal-backdrop';wrap.innerHTML=`<div class="modal"><div class="card-title"><h3>Create monitor</h3><button class="btn btn-sm" data-close>×</button></div><form id="monitor-form" class="form-grid"><div class="field"><label>Name</label><input class="input" name="name" required></div><div class="field"><label>Type</label><select class="select" name="type"><option>web</option><option>api</option><option>mcp</option><option>gaming</option></select></div><div class="field"><label>Target</label><input class="input" name="target" required placeholder="https://… or game identity"></div><div class="field"><label>Interval</label><select class="select" name="interval_minutes"><option value="15">15 minutes</option><option value="60" selected>1 hour</option><option value="360">6 hours</option><option value="1440">Daily</option></select></div><button class="btn btn-primary">Create</button></form></div>`;document.body.appendChild(wrap);wrap.querySelector('[data-close]').onclick=()=>wrap.remove();wrap.querySelector('#monitor-form').onsubmit=async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget).entries());f.interval_minutes=Number(f.interval_minutes);try{await api('/api/monitors',{method:'POST',body:f});wrap.remove();dashMonitors()}catch(err){toast(err.message)}}}
async function dashConnections(){let s={};try{s=await api('/api/status')}catch{}const providers=[['Firecrawl','Search · scrape · map · crawl'],['Apify','Actors · datasets'],['Composio','Connected app tool catalog'],['RapidAPI','Manifest providers'],['OpenAPI','Dynamic GET/HEAD catalogs'],['Remote MCP','Federated MCP servers'],['Gaming Intelligence','Multi-game public profiles'],['Sandbox / Browser','Cloud computer plane']];dashboardShell('connections',`<div class="page-head"><div><h1>Connections</h1><p>Provider capability planes. Credentials stay server-side.</p></div></div><div class="provider-grid">${providers.map(([a,b])=>`<div class="provider"><div><strong>${a}</strong><small>${b}</small></div><span class="pill good">Available</span></div>`).join('')}</div><div style="height:16px"></div><div class="card"><div class="card-title"><h3>Runtime configuration</h3></div><p class="muted small">Control DB: ${s.control_database?'configured':'pending'} · Billing: ${s.billing?'configured':'pending'} · GitHub OAuth: ${s.github_oauth?'configured':'pending'}</p></div>`)}
async function dashMcp(){const endpoint=`${location.origin}/mcp`;dashboardShell('mcp',`<div class="page-head"><div><h1>MCP connection</h1><p>The canonical endpoint stays stable while providers and capabilities evolve.</p></div></div><div class="card"><div class="card-title"><h3>Permanent MCP URL</h3><span class="pill good">Streamable HTTP</span></div><div class="endpoint"><code>${endpoint}</code><button class="btn btn-sm" id="copy-mcp">Copy</button></div><h3 style="margin-top:28px">Recommended connection</h3><p class="muted">Add this URL to an OAuth-capable MCP client. The gateway advertises its authorization metadata, opens the Internet Hands authorization screen, validates your API key, and gives the client a short-lived bearer token.</p><pre>GET /.well-known/oauth-protected-resource\n→ authorization server\n→ /oauth/authorize\n→ API key approval\n→ /oauth/token\n→ short-lived Bearer token</pre><h3>Direct API key</h3><pre>Authorization: Bearer ih_live_…</pre></div><div style="height:16px"></div><div class="card"><div class="card-title"><h3>Agent calling style</h3></div><pre>1. mesh_route("what I need")\n2. mesh_describe(ref)       # only when schema is unknown\n3. mesh_execute(ref, input)\n4. mesh_job_status(id)      # async work\n5. mesh_results(id)         # page large results</pre></div>`);qs('#copy-mcp').onclick=()=>copyText(endpoint)}
function dashSettings(){const u=state.me?.user||{};dashboardShell('settings',`<div class="page-head"><div><h1>Settings</h1><p>Account and security configuration.</p></div></div><div class="card" style="max-width:620px"><div class="form-grid"><div class="field"><label>Email</label><input class="input" value="${esc(u.email||'')}" disabled></div><div class="field"><label>Display name</label><input class="input" value="${esc(u.display_name||'')}" disabled></div><div class="callout">Profile editing, active-session management and account deletion hooks are reserved for the next control-plane pass. API keys can already be revoked independently.</div></div></div>`)}
async function renderRoute(){const path=location.pathname;window.scrollTo(0,0);try{if(path.startsWith('/dashboard'))return await renderDashboard();if(path==='/pricing')return await renderPricing();if(path==='/status')return await renderStatus();if(path.startsWith('/docs'))return renderDocs();if(path==='/login')return renderAuth('login');if(path==='/signup')return renderAuth('signup');return await renderHome()}catch(e){console.error(e);publicShell(`<main class="section"><div class="container"><div class="empty">${esc(e.message||'Something went wrong')}</div></div></main>`);wireLinks()}}
window.addEventListener('popstate',renderRoute);document.addEventListener('click',e=>{const a=e.target.closest('[data-link]');if(!a)return;if(e.metaKey||e.ctrlKey||e.shiftKey)return;e.preventDefault();go(a.getAttribute('href'))});renderRoute();
